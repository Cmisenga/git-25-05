git --version
mkdir git-25-05
cd git-25-05/
git init
ls -la
cd .git
ls -l
cd ..
python3 --version
nano first-code.py
python3 first-code.py
git status
git add first-code.py
git status
git config --global user.name "vikas99341"
git config --global user.email "vikasisinlove@gmail.com"
git config --list
git commit -m "First commit to python code "

git branch -M main
git remote add origin https://github.com/vikas99341/git-25-05.git
git push -u origin main

Generate the personal access token from setting -- > developer setting -- > personal access token -- save 

git config --global user.name "vikas99341"
git config --global user.email "vikasisinlove@gmail.com"
git config --list  - to congiure your git hub with git 
git branch - To check on which branch you are 
git branch -M main -  To make main as my default branch instead of master
git remote add origin https://github.com/vikas99341/git-15-02.git - Adding remote path
git remote -v - Checking the remote path
git push -u origin main - Pushing the chages from local to git hub 
git fetch - Just tell me if i have the latest content
git pull origin main - pull the latest content from git hub in case i am lagging
git clone - Remove the old content and put everything again 
to merge the change do git pull and avoid merge conflict.
